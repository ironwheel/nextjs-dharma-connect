{
    "buildCommand": "pnpm --filter=email-manager build",
    "outputDirectory": "apps/email-manager",
    "installCommand": "pnpm install",
    "framework": "nextjs",
    "functions": {
        "apps/email-manager/pages/api/**/*.ts": {
            "runtime": "nodejs18.x"
        }
    }
}