{
    "buildCommand": "pnpm --filter=email-manager build",
    "outputDirectory": "apps/email-manager/.next",
    "installCommand": "pnpm install",
    "framework": "nextjs",
    "rootDirectory": "apps/email-manager",
    "functions": {
        "apps/email-manager/pages/api/**/*.ts": {
            "runtime": "nodejs18.x"
        }
    }
}